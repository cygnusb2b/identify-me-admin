{{#-bs/row}}
  {{#-bs/col}}
    <h3 class="mb-0 float-left">{{ model.type }}</h3>
    {{#-bs/button "danger" class="float-right" size="small" outline=true click=(route-action 'softDelete' model 'campaign.email-signup.index')}}{{-ion 'android-close'}} Delete{{/-bs/button}}
  {{/-bs/col}}
{{/-bs/row}}
<hr>
{{#-bs/row}}
  {{#-bs/col}}
    <h4>
      {{ model.name }}
      {{#if model.promoCode}}
        ({{model.promoCode}})
      {{/if}}
    </h4>
    <h5 class="mb-0">{{ model.service.name }}</h5>
  {{/-bs/col}}
{{/-bs/row}}
<hr>
{{campaign/-manage campaign=model routeName="campaign.email-signup.index"}}
